query getNodeStatus($path: String!) {
    jcr {
        nodeByPath(path: $path) {
            mixins {
                name
            }
            properties(names: ["customGptPageId"]) {
                name
                value
            }
        }
    }
}
