mutation createPage($parentPath: String!, $name: String!) {
    jcr {
        addNode(parentPathOrId: $parentPath, primaryNodeType: "jnt:page", name: $name) {
            path
            uuid
        }
    }
}
